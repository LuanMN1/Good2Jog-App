<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Cor de fundo -->
    <solid android:color="#2196F3" />

    <!-- Borda arredondada -->
    <corners android:radius="16dp" />

    <!-- Sombra sutil (opcional, para API < 21 precisa elevation no LinearLayout) -->
    <padding
        android:left="0dp"
        android:top="0dp"
        android:right="0dp"
        android:bottom="0dp"/>
</shape>
